{% extends 'base.html.twig' %}
  
{% block title %}Edit Season{% endblock %}
  
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Edit Season</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_season_index') }}"> 
                    View All Seasons
                </a>
            </div>
            <div class="card-body">
                {% include 'season/_form.html.twig' with {'button_label': 'Update Season', 'button_color': 'btn-outline-success'}  %}
            </div>
        </div>
    </div>
{% endblock %}